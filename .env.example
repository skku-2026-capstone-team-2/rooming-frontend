# Tmap API key
VITE_TMAP_APP_KEY=

# 실제 서버 base URL (기본값: http://localhost:8080)
VITE_API_BASE_URL=http://localhost:8080

# mock ↔ real API 전환
# 이 값이 "false"일 때만 실제 서버를 호출한다. 그 외엔 mock 어댑터 사용.
VITE_USE_MOCK=true
