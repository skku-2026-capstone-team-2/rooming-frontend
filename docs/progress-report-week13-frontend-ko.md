# Team #2 진행 보고서 - Week #13

## 지난 2주간의 목표는 무엇이었는가?

### 프론트엔드 파트 - Yejin

- 기존 초기 프로토타입 수준의 MVP 구조를 라우트, 페이지, 컴포넌트 기반의 프론트엔드 구조로 리팩토링한다.
- 초기 MVP 화면 범위를 넘어 보다 완성된 프로토타입 사용자 흐름으로 확장한다.
- 온보딩, AI 추천 결과, 매물 상세, 인프라 보기, 3D 보기, 관리자 프로토타입 화면을 각각 독립적인 화면으로 구현한다.
- 백엔드 서버가 아직 배포되지 않은 상황을 고려하여, OpenAPI 명세 기반의 백엔드 연동 준비를 진행한다.
- 화면 내부에 직접 하드코딩된 데이터를 향후 백엔드 API 응답과 매핑할 수 있는 mock 데이터 구조로 정리한다.
- midterm 프로토타입 이후 사용자 흐름, 화면 이동, 시각적 브랜딩의 일관성을 개선한다.

## 이번 주에 완료한 목표는 무엇인가?

### 프론트엔드 파트

- 기존 단일 MVP 수준의 구조에서 벗어나 React Router 기반의 라우트 중심 구조로 프론트엔드를 재구성했다.
- 온보딩, 지도, AI 결과, 매물 상세, 인프라 보기, 3D 보기 화면을 연결하여 사용자 흐름을 구현했다.
- 백엔드 배포 전 프론트엔드 흐름을 검증하기 위해 매물, 찜 매물, 인프라, 통학 경로 데이터를 mock 기반으로 구성했다.
- 추천 매물과 MY 매물을 전환할 수 있는 매물 리스트 모드를 구현했다.
- 지도 화면에서 매물 마커 표시 여부를 제어할 수 있는 기능을 추가했다.
- 카테고리, 검색 반경, 키워드 기반 인프라 검색 UI를 추가했다.
- Tmap 마커와 거리/경로 표시 로직을 활용하여 인프라 지도 시각화를 구현했다.
- Spline iframe과 평면도 전환 기능을 활용한 3D 방 보기 화면을 구현했다.
- 매물 관리 UI를 탐색하기 위한 관리자 프로토타입 화면을 추가했다.
- `npm run build`를 통해 프로젝트가 정상적으로 빌드되는 것을 확인했다.

## 완료하지 못한 목표에 대한 비판적 회고

### 프론트엔드 파트

현재 백엔드는 OpenAPI 문서는 존재하지만 실제 서버가 아직 배포되지 않은 상태이기 때문에, 프론트엔드는 아직 실제 백엔드 서버와 연결되지 않았다. 이로 인해 매물 데이터, 추천 결과, 사용자 선호 조건, 찜 매물, 인프라 데이터, 3D 모델 URL에 대한 실제 API 연동은 완료하지 못했다.

대신 현재 프론트엔드는 mock 데이터와 dummy API 형태의 구조를 활용하여 end-to-end 사용자 흐름을 검증하고 있다. 다음 단계에서는 OpenAPI 명세에 맞춰 프론트엔드 타입과 API client 구조를 정리하여, 백엔드 서버가 배포된 이후 빠르게 실제 연동이 가능하도록 준비해야 한다.

또한 production build는 성공하지만, lint 오류와 타입 경고가 아직 남아 있다. 실제 통합 단계에 들어가기 전 프론트엔드 안정성을 높이기 위해 이러한 코드 품질 문제를 정리할 필요가 있다.

## 앞으로 2주간의 목표는 무엇인가?

### 프론트엔드 파트

- OpenAPI 문서를 분석하고 이에 대응하는 TypeScript 타입을 정의한다.
- 매물, 추천, 인프라, 찜, 사용자 선호 조건, 3D 데이터에 대한 API client 모듈을 생성한다.
- 현재 mock 데이터를 OpenAPI 응답 형식과 호환되는 mock response 구조로 전환한다.
- 화면이 하드코딩 데이터를 직접 import하는 방식에서 벗어나 mock API 함수를 통해 데이터를 소비하도록 리팩토링한다.
- 향후 실제 API 호출에 대비하여 loading, error, empty 상태를 추가한다.
- AI 검색 흐름을 mock 추천 API 구조와 연결한다.
- 매물 상세, 인프라 보기, 3D 보기 화면을 property ID 기반 mock API 응답으로 연결한다.
- lint 오류를 수정하고 TypeScript 경고를 줄인다.
- 리팩토링 이후에도 `npm run build`가 계속 통과하도록 유지한다.

## 위 목표별 소요 시간

### 프론트엔드 파트

- 라우트/페이지/컴포넌트 구조 리팩토링: 3시간
- midterm 이후 확장된 사용자 흐름 구현: 5시간
- 매물 상세, 인프라 보기, 3D 보기 화면 개선: 5시간
- mock 데이터 및 dummy API 형태의 흐름 준비: 3시간
- 지도 마커, 인프라, 경로 시각화 작업: 3시간
- UI/UX 및 브랜딩 일관성 개선: 4시간
- 빌드 검증 및 이슈 확인: 1시간
- OpenAPI 기반 연동 계획 수립: 2시간
